import SalaryData from "@/components/SalaryData";

export const metadata = {
  title: "Salary Data | HireLoop",
  description:
    "Explore real salary benchmarks by role, experience, and industry across Bangladesh.",
};

export default function SalaryPage() {
  return <SalaryData />;
}
