import Newsroom from "@/components/Newsroom";

export const metadata = {
  title: "Newsroom | HireLoop",
  description:
    "Product launches, milestones, and press coverage from the HireLoop team.",
};

export default function NewsroomPage() {
  return <Newsroom />;
}
