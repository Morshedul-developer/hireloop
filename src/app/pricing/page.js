import Pricing from "@/components/Pricing";

export const metadata = {
  title: "Pricing | HireLoop",
  description: "Choose the HireLoop plan that fits your career or hiring goals.",
};

export default function PricingPage() {
  return <Pricing />;
}
